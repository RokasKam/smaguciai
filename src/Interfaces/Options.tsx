export interface Category {
  id: string;
  name: string;
  amountOfProducts: number;
}

export interface Manufacturer {
  id: string;
  name: string;
  amountOfProducts: number;
  country: string;
  phoneNumber: string;
  address: string;
}
